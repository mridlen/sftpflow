/target

# Editor/tool temp files (e.g. *.tmp.<pid>.<timestamp> left by the
# Claude Code Edit tool on Windows when Dropbox/AV blocks the replace).
*.tmp.*

# Test-env SSH keys generated by `make test-keygen`. Trusted only
# by the disposable docker/compose.test.yml container — never commit.
/docker/test-keys/
